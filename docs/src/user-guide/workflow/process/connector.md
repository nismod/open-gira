# Process connector

We have now processed and defined the network for each box. This is beneficial for parallel processing capabilities but
we still need to know the relations between the boxes. This file examines each box and documents the connection between
the networks. This information is later used when finding the extent of the network which contains a damaged component.